// HomeScreen removed — kept placeholder file to avoid breaking imports during
// incremental edits. The app now navigates directly to `DashboardScreen` after
// login. Remove this file entirely when safe to do so.
// HomeScreen removed — kept placeholder file to avoid breaking imports during
// incremental edits. The app now navigates directly to `DashboardScreen` after
// login. Remove this file entirely when safe to do so.


