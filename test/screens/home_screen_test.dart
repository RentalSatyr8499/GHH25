import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // HomeScreen has been removed; keep a placeholder test so CI doesn't break
    expect(1 + 1, 2);
  });
}
